name: todolist
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.5+1
  shared_preferences: ^2.5.3
  table_calendar: ^3.2.0
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.13.1

# ตั้งค่าไอคอนแอป (ชี้ไปที่โฟลเดอร์ราก: assets_images/)
flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets_images/icon_todolist.png"
  min_sdk_android: 21
  remove_alpha_ios: true

flutter:
  uses-material-design: true
